\section{Introduction}

GPU resource management policies critically influence application performance. These policies include memory placement, migration, and GPU task scheduling.  Recent research shows their substantial impact on Unified Virtual Memory (UVM) placement~\cite{wang2024suv,lin2025forest,kim2025dream,park2025helm} and GPU scheduling strategies~\cite{wang2024gcaps,fan2025gpreempt,shen2025xsched}. For instance, optimal eviction and prefetch policies can improve performance by up to 73\% under memory oversubscription~\cite{park2025helm,kehne2019etc,ganguly2019interplay}.

Unfortunately, finding an optimal GPU resource management policy is challenging. The optimal memory and scheduling policies vary significantly across workloads, hardware, and deployment contexts~\cite{kwon2023efficient,agrawal2024taming,lin2024towards,wang2016gunrock,shoeybi2019megatron,rasley2020deepspeed,pan2024survey,cao2023gpu}, with no one policy dominating the others.  For example, aggressive memory prefetching improves latency for LLM MoE inference by reducing memory stalls~\cite{park2025helm,ganguly2019interplay}, but increases the latency of graph analytics queries because it over-saturates memory bandwidth~\cite{ganguly2019interplay}.  Additionally, throughput-oriented scheduling improves average latency of vector search but degrades tail latency for interactive inference~\cite{agrawal2024taming,fan2025gpreempt}.

%\arq{I don't love this sentence structure.}
We argue that the community should employ AI agents to automatically optimize programmable GPU resource management policies.  AI-driven exploration can handle the vast, workload-specific policy space that makes manual tuning challenging.
% Vast, workload-specific policy space make manual tuning costly but suitable for AI-driven exploration. 
Agents can now generate complex code and optimize policies in domains such as compiler heuristics~\cite{alphaevolve2025}, datacenter scheduling, LLM serving~\cite{hamadanian2025glia,cheng2025barbarians}, training strategies~\cite{ding2025asap}, and CPU scheduling~\cite{dwivedula2025policysmith,zheng2025schedcp}.  In these use-cases, agents are generating fully fledged policy programs rather than simply tuning configuration parameters.

We derive three properties that systems should provide to enable AI agents to optimize programmable GPU resource management policies. A policy interface should be safe by constraining policy memory accesses and validating requests before trusted code modifies resources; dynamic by supporting policy updates without system or application restarts; and full-stack by exposing cross-application and device-internal state together with low-level controls. Safety here concerns the covered program and transition contracts, not freedom from GPU faults, resource exhaustion, or performance regressions.

Existing approaches expose different parts of this interface. User-space frameworks~\cite{ng2023paella,shen2025xsched,gim2025pie,chen2025ktransformers} can coordinate multiple applications, but do not directly expose fault-time UVM or PMM transitions that the driver keeps internal. Driver modifications~\cite{kato2011timegraph,kato2012gdev,kernelintercept,fan2025gpreempt,wang2024gcaps} can expose those controls, but require their own policy-update and validation paths. The distinction is the available event, actuator and trust boundary, not that user-space policies are necessarily local to one application or driver modifications are inherently unsafe.

In this paper, we propose \sys, an event-driven OS interface for GPU resource-management policies. Similar to prior work~\cite{schedext-docs, zussman2025cache_ext, bachl2021flow}, \sys{} supports eBPF callbacks on selected events. Kernel-loaded policies use Linux's verifier, while typed transition requests preserve driver-owned validation and actuation. A separate device-verifier prototype checks base-program and SIMT constraints. The interface spans host-driver and GPU-device execution, but these paths have different verification and deployment boundaries (\S\ref{sec:verification}).

\sys models GPU resource management as transitions on resource state machines (\S\ref{sec:design-interfaces}).  State machines define valid states and transitions: GPU memory chunks transition between host memory and device memory through eviction and prefetching; compute contexts transition through states such as running, ready, and finished via scheduling and preemption operations.  eBPF policies specify the transitions that the system should take on its GPU resources.



%, with a SIMT-aware verifier and device JIT runtime. Bring these up in the implementation paragraph.

Alas, applying the CPU eBPF programming model to GPU resource management is not straightforward. The GPU is a discrete processor with its own execution model and resources, and policy decisions made in the host take effect on a separate device. In particular, we identify three challenges: first, cross-device operations like migrating data or dispatching commands take milliseconds, too slow for synchronous callbacks and too costly for purely reactive decisions. Second, unlike independent subsystems on the CPU, limited device memory and bandwidth further couple memory and scheduling, as decisions about data placement directly affect which workloads can execute. Third, GPU applications employ OS kernel bypass, which requires \sys{} to support eBPF callbacks from events in user-space applications. Traditional eBPF does not allow such callbacks to modify OS state required to implement resource management policies because of safety concerns.

\sys{} addresses these challenges through an execution model for eBPF-based resource management that separates the effect of a callback from its triggering event. Its callbacks can request asynchronous resource transitions at user-defined events in applications, the OS kernel driver, or the GPU device. A callback may request multiple transitions; the driver retains responsibility for resource ownership, synchronization and actuation. For the covered driver-owned operations, typed requests and transition validation prevent policies from bypassing the checked state-machine boundary. Device-side memory and SIMT checks are a separate contract, with the enforcement limitations described below.

% To ensure safety, policies only influence which transitions occur and when, but cannot create invalid transitions.


We implement \sys{} on Linux by extending the NVIDIA open GPU OS kernel modules~\cite{nvidia-open-gpu} and providing an eBPF SIMT verifier and JIT runtime for GPU devices. We evaluate \sys{} across LLM inference, GNN training, vector search, and multi-tenant scenarios. In the original driver-policy case studies, \sys-based policies improve throughput by up to $4.8\times$ over framework offloading and $1.76\times$ over application-tuned baselines, and reduce tail latency by up to $2\times$. Policy updates require no additional application-source changes beyond the stated UVM setup, or application restarts.
These headline improvements compare workload-specific policies with baseline policies, not the same algorithm on two execution mechanisms. We separately measure a 3.219\% kernel-time cost for expressing the same no-prefetch policy through the UVM hook, and compare native/BPF implementations of MoE-Infinity, XSched, GPreempt, Expert Buffering, FineMoE, Hummingbird and POD components (\S\ref{sec:policy-mechanism}). Their bounded differences are not formal equivalence, and some policy ports lose throughput to simpler baselines. The agent-exploration records document GPU faults and system OOM but no OS kernel panic; we evaluate verifier rejection, transition validation and workload outcomes separately. In summary, our contributions are:

\begin{itemize}[leftmargin=*,itemsep=0pt]
  \item an execution model for eBPF-based resource management that separates the effect of a callback from its triggering event.
  %We design a GPU-specific OS policy interface that models GPU resources as state machines whose asynchronous state transitions are directed by verified eBPF programs across the host-device boundary.
  \item \sys, a programmable GPU resource-management interface spanning host and device, with dynamic policy updates, checked driver transitions and an explicitly scoped device-verifier prototype.
  %We implement \sys, an eBPF runtime spanning host driver and GPU device, with a SIMT-aware verifier for device-side eBPF execution.
  \item an evaluation of workload-specific policies, including up to $4.8\times$ throughput improvement and $2\times$ tail latency reduction, together with same-policy mechanism costs and explicitly scoped comparisons to existing policy implementations.
\end{itemize}
