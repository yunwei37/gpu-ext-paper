\subsection{RQ5: Policy Expressibility and Matched Implementations}
\label{sec:policy-mechanism}

We evaluate whether \sys{} can express the policies of published state-of-the-art systems, and what the general mechanism costs when it runs the same policy as an ad-hoc implementation.
% 我们评估 \sys{} 能否表达已发表最先进系统的策略，以及通用机制在运行与临时实现相同的策略时代价几何。
\Cref{tab:policy-expressibility} maps each policy class to its feasibility in user space, with driver modifications, and in \sys{}.
% \Cref{tab:policy-expressibility} 将每个策略类别映射到其在用户态、驱动修改和 \sys{} 下的可行性。

\begin{table*}[t]
\centering
\small
\begin{tabularx}{\textwidth}{@{}p{0.20\textwidth}XXX@{}}
\toprule
Decision class & User space & Driver changes & \sys{} path \\
\midrule
Expert prediction/prefetch & Framework routing/transfers & Unneeded for application-owned transfers & Host-JIT selectors; semantic adapters \\
Priority queue admission & Cooperative XSched Level-1 & Unneeded for Level-1 & Host-JIT HPF; original suspend/resume \\
TSG timeslices & Scoped custom C API & Required API/hooks & Kernel BPF; native GSP actuator \\
Fault-time prefetch & Explicit prefetch is not fault handling & Expose fault decisions & Kernel BPF; bounded current-VA-block selection \\
UVM eviction-list order & Ordinary hints lack this control & Expose PMM lists & Kernel BPF; validated used/unused head/tail, not arbitrary victims \\
SM-local task selection & Cooperating fused kernel & Unneeded for its task queue & Device-BPF POD selector; explicit context/launch bridge \\
Disk/CXL placement & Framework-controlled I/O & Destination/lifetime/completion interfaces & No general storage/CXL backend \\
\bottomrule
\end{tabularx}
\caption{Policy expressibility by decision class. The available observations and actuators determine what each layer can express.}
% 按决策类别划分的策略可表达性。可用的观测与执行器决定了每一层能表达什么。
\label{tab:policy-expressibility}
\end{table*}

\paragraph{Methodology}
% 方法
Each comparison pairs the original implementation with a \sys{} port of the same policy on the same workload and executor, on RTX~5090 (driver 575.57.08) with exclusive GPU access and randomized complete blocks.
% 每组对比在相同工作负载和执行器上，将原始实现与同一策略的 \sys{} 移植版配对，在 RTX~5090（驱动 575.57.08）上以独占 GPU 和随机完整区块运行。
All arms pass numerical correctness and policy-engagement checks; we report medians with paired bootstrap intervals.
% 所有实验臂都通过数值正确性和策略生效检查；报告中位数及配对 bootstrap 区间。
The ports measure policy expressibility and mechanism cost on our hardware; they do not reproduce each system's original hardware or headline numbers.
% 这些移植在我们的硬件上测量策略可表达性与机制代价；并不复现各系统原有的硬件或头条数字。

\paragraph{MoE-Infinity (expert offloading)}
% MoE-Infinity（专家卸载）
We port MoE-Infinity's~\cite{xue2025moeinfinity} activation-aware prediction, prefetch ranking, and score-based eviction to host-JIT \sys{} selectors sharing the same GPT-OSS-120B frontend.
% 我们将 MoE-Infinity~\cite{xue2025moeinfinity} 的激活感知预测、预取排序和基于分数的驱逐移植到共享同一 GPT-OSS-120B 前端的 \sys{} 主机 JIT 选择器。
Over five blocks, baseline, native, and \sys{} achieve 11.90, 11.22, and 11.19 tokens/s: the \sys{} port tracks the native policy within 0.4\% while cutting first-visible-text latency by 22.5\% relative to baseline.
% 五个区块下，基线、原生实现和 \sys{} 分别达到 11.90、11.22 和 11.19 token/s：\sys{} 移植版与原生策略的差距在 0.4\% 以内，同时相对基线将首个可见文本延迟降低 22.5\%。

\paragraph{XSched (preemptive scheduling)}
% XSched（抢占式调度）
On XSched's~\cite{shen2025xsched} public Level-1 inter-kernel preemption, a host-JIT \sys{} program makes the bounded highest-priority-first decision while the original executor performs suspend/resume.
% 在 XSched~\cite{shen2025xsched} 公开的 Level-1 内核间抢占上，主机 JIT 的 \sys{} 程序做出有界的最高优先级优先决策，由原始执行器执行挂起/恢复。
LC submission-to-first-CTA p99 is 76.9~s native, 27.0~s with XSched, and 27.3~s with the \sys{} port (paired difference $+0.29$~s, CI $[-0.11,+0.64]$), with BE throughput unchanged.
% LC 提交到首个 CTA 的 p99：原生 76.9 秒，XSched 27.0 秒，\sys{} 移植版 27.3 秒（配对差 $+0.29$ 秒，CI $[-0.11,+0.64]$），BE 吞吐量不变。

\paragraph{GPREEMPT (priority timeslicing)}
% GPREEMPT（优先级时间片）
Under continuous background load, the GPREEMPT~\cite{fan2025gpreempt} policy reduces LC response p99 from 1.796~ms (native) to 1.615~ms (original C) and 1.610~ms (\sys{}), at about 9\% background-throughput cost in both implementations; the \sys{} port differs from the original C by $-1.3\%$ (CI $[-4.2\%,+0.5\%]$).
% 在连续后台负载下，GPREEMPT~\cite{fan2025gpreempt} 策略将 LC 响应 p99 从 1.796 毫秒（原生）降至 1.615 毫秒（原始 C 实现）和 1.610 毫秒（\sys{}），两种实现都付出约 9\% 的后台吞吐量代价；\sys{} 移植版与原始 C 的差异为 $-1.3\%$（CI $[-4.2\%,+0.5\%]$）。
A knee sweep brackets full foreground coverage at 625 requests/s.
% 拐点扫描在每秒 625 个请求处界定了前台全覆盖的范围。

\paragraph{Expert Buffering (expert residency)}
% Expert Buffering（专家驻留）
Porting Expert Buffering's~\cite{huang2023towards} inactive-first hot-expert residency to the same executor improves throughput over a fixed-$K$ FIFO baseline by 2.6\% (native) and 1.8\% (\sys{}), with identical residency decisions and 12.9\% less copy payload; the \sys{} path costs 0.7\% relative to the native policy.
% 将 Expert Buffering~\cite{huang2023towards} 的非活跃优先热专家驻留策略移植到同一执行器，相比固定 $K$ FIFO 基线吞吐量提升 2.6\%（原生）和 1.8\%（\sys{}），驻留决策完全一致且拷贝负载减少 12.9\%；\sys{} 路径相对原生策略的代价为 0.7\%。

\paragraph{FineMoE (dynamic expert sets)}
% FineMoE（动态专家集）
The \sys{} port of FineMoE's~\cite{yu2026finemoe} dynamic expert-set selection reduces prefetched-but-unused payload by 60.4\% over an all-positive ablation and matches its native counterpart (4.51 vs 4.50 tokens/s).
% FineMoE~\cite{yu2026finemoe} 动态专家集选择的 \sys{} 移植版相比全正例消融减少了 60.4\% 的预取但未用负载，并与原生实现持平（4.51 对 4.50 token/s）。

\paragraph{Hummingbird (idle admission)}
% Hummingbird（空闲准入）
Both the C and \sys{} ports of Hummingbird's~\cite{hu2026hummingbird} idle-admission component behave identically (\sys{}/C ratio 0.994), and both lose 19--20\% background throughput to fixed timeslicing---a policy-level trade-off that the mechanism reproduces faithfully.
% Hummingbird~\cite{hu2026hummingbird} 空闲准入组件的 C 版和 \sys{} 移植版表现一致（\sys{}/C 比率 0.994），两者相比固定时间片都损失 19--20\% 的后台吞吐量——这是策略层面的取舍，机制如实地复现了它。

\paragraph{POD-Attention (device-side selection)}
% POD-Attention（设备端选择）
A device-BPF selector preserves POD-Attention's~\cite{kamath2025pod} proportional SM-local task choice with exact numerical output, adding 1.78\% mean operator latency over the CUDA adapter across ten attention shapes and beating two-stream FlashAttention in seven of them.
% 设备端 BPF 选择器保持了 POD-Attention~\cite{kamath2025pod} 按比例的 SM 本地任务选择，数值输出完全一致；在十种注意力形状上，相比 CUDA 适配器平均算子延迟增加 1.78\%，并在其中七种形状上优于双流 FlashAttention。

\paragraph{Mechanism cost}
% 机制代价
Across the matched ports, executing the same policy through \sys{} costs 0.7--3.3\%: 0.7\% for Expert Buffering, 1.8\% for POD-Attention's device selector, and 3.2\% for a kernel-verified UVM no-prefetch policy measured over fifteen paired fault-path blocks.
% 在所有匹配移植中，通过 \sys{} 执行相同策略的代价为 0.7--3.3\%：Expert Buffering 为 0.7\%，POD-Attention 设备端选择器为 1.8\%，经内核验证的 UVM 无预取策略在十五个配对缺页区块上测得 3.2\%。
The headline improvements in RQ1--RQ3 are therefore policy gains, obtainable through a safe, dynamic, and full-stack mechanism at single-digit cost.
% 因此，RQ1--RQ3 中的头条提升是策略收益，可以通过一个安全、动态、全栈的机制以个位数的代价获得。

\paragraph{Agent-discovered policies}
% 智能体发现的策略
Beyond reproducing published policies, the mechanism enables policies that no existing system implements.
% 除了复现已发表的策略，该机制还使能了任何现有系统都未实现的策略。
In the MoE study (\S\ref{sec:eval}), device-side tracing led the agent to a stride-prefetch policy aligned with per-layer expert weight layout combined with LFU eviction that protects shared layers; in the KV-cache study, the agent differentiated prefetch by region type (stride for weights, sequential for KV-cache) under a PCIe-utilization budget, avoiding the mutual thrashing that a uniform policy causes.
% 在 MoE 研究（\S\ref{sec:eval}）中，设备端追踪引导智能体得出与每层专家权重布局对齐的步长预取策略，并结合保护共享层的 LFU 驱逐；在 KV-cache 研究中，智能体在 PCIe 利用率预算下按区域类型区分预取（权重用步长、KV-cache 用顺序），避免了统一策略导致的相互抖动。
These policies depend on cross-layer observations---device-side access patterns driving host-side migration---that framework-level mechanisms cannot express.
% 这些策略依赖跨层观测——由设备端访问模式驱动主机端迁移——这是框架层机制无法表达的。
