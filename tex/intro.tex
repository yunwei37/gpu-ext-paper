\section{Introduction}

% \arq{My high level feedback is that many of these paragraphs are doing too much.  Each paragraph should have a single key idea and support for that idea. Many of the paragraphs below cover more than one idea, which makes it challenge to follow as a reader of the paper.  A simple test for you to follow: you should be able to read only the topic sentence from each paragraph (usually, this is the first sentence) and have that make sense.  It does not work that way in this paper.}
% \yusheng{Resolved: all paragraphs split to one idea each.}

% \arq{This paragraph is trying to say two things at once: (1) that GPU resource management policies are critical and that (2) they are application/workload specific.  I would split these into two paragraphs. The first paragraph would have the topic sentence "GPU resource management policies critically influence application performance".  It would define what we mean by GPU resource management.  It would then cite the papers that you've included here (eg, the ones on scheduling and UVM placement).  The second paragraph would have the topic sentence "Unfortunately, finding an optimal GPU resource management policy is challenging".  It would then talk about how the ideal policy depends upon the deployed system and workload, and how an optimal policy for one system can substantially harm another.  (It might also talk about how find the right policy is challenging because you have to do kernel programming?)}

GPU resource management policies critically influence application performance. These policies include memory placement, memory migration, and GPU task scheduling.  Recent research shows their substantial impact on Unified Virtual Memory (UVM) placement~\cite{wang2024suv,lin2025forest,kim2025dream,park2025helm} and GPU scheduling strategies~\cite{wang2024gcaps,fan2025gpreempt,shen2025xsched}. For instance, optimal eviction and prefetch policies can improve performance by up to 73\% for systems when under memory oversubscription~\cite{park2025helm,kehne2019etc,ganguly2019interplay}.

Unfortunately, finding an optimal GPU resource management policy is challenging. The optimal memory and scheduling policies vary significantly across workloads, hardware, and deployment contexts~\cite{kwon2023efficient,agrawal2024taming,lin2024towards,wang2016gunrock,shoeybi2019megatron,rasley2020deepspeed,pan2024survey,cao2023gpu}, with no one policy dominating the others.  For example, aggressive memory prefetching reduces stalls in LLM MoE inference but wastes bandwidth in graph analytics, while throughput-oriented scheduling improves vector search but worsens tail latency for interactive inference.\arq{A citation would be critical}

% \arq{This paragraph is mostly good, up until the last sentence.  The beginning of the paragraph is saying "Agents would be great for this task"; that's the point of the paragraph.  But, the last sentence adds this new idea that is easy to miss: that we need new OS interfaces for this task.  It does not define what those properties are (dynamic, programmable, and safe). It is also redundant ("without restarting applications" is equivalent to dynamic, I think?).  It is a separate idea! So, this needs to be two paragraphs.}

We argue that the community should employ AI agents to automate the process of optimizing GPU resource management policies.  Agents can now generate complex code and optimize policies in domains such as compiler heuristics~\cite{alphaevolve2025}, datacenter scheduling, LLM serving~\cite{hamadanian2025glia,cheng2025barbarians}, training strategies~\cite{ding2025asap}, and CPU scheduling~\cite{dwivedula2025policysmith,zheng2025schedcp}.  In these use-cases, agents are generating fully fledged policy programs rather than simply tuning configuration parameters.

%We are entering an age where AI agents increasingly optimize resource management policies. Finding optimal GPU resource management policies requires exploring a vast, workload-specific policy space, making manual tuning costly but suitable for AI-driven exploration.

We derive three properties that systems should provide to enable AI agents to optimize GPU resource management policies effectively without sacrificing system health.  Namely, a system should support programmable policy interfaces that are safe, dynamic and full-stack.  A policy interface is safe if it ensures that new policies cannot cause the system to crash; dynamic if it supports policy updates without requiring the system and applications to be restarted; and full-stack if it supports cross-application visibility and low-level hardware control. 

% To enable agents to optimize GPU resource management policies, systems should support programmable policy interfaces that are safe, dynamic, and full-stack. 

%,  programmable interfaces that are dynamic, safe, and full-stack. To leverage agent-driven exploration, interfaces need to be dynamic to support runtime policy updates without application restarts and service interrupt, programmable to express complex policies, and safe to enforce OS kernel isolation. To effectively manage resources across tenants, interfaces need to provide full-stack cross-application visibility and low-level hardware control, such as page fault handling and precise command scheduling.

% \arq{The flow into this paragraph is totally broken.  The last idea in the paper was about what an OS interface needs, and not we just jump into a (totally different) tradeoff imposed by existing GPU resource management approaches.  The ideas need to be connected; like, wherever we left off previously (ie "we need an interface with three properties...") should be where this paragraph picks up.  One possible topic sentence is "Alas, existing approaches fail to simultaneously meet all three requirements for GPU resource management."}

Existing programmable GPU resource management techniques fail to simultaneously meet these requirements. User-space frameworks~\cite{ng2023paella,shen2025xsched,gim2025pie,chen2025ktransformers} are safe and dynamic but are not full-stack since policies lack cross-application visibility and hardware control.  Other approaches modify the kernel driver~\cite{kato2011timegraph,kato2012gdev,kernelintercept,fan2025gpreempt,wang2024gcaps}, which is full-stack but neither safe nor dynamic.  

%Fixed configuration knobs exposed by existing APIs are safe but are not programmable.  We ignore these becuase we only care about programmability...?

% \arq{We've given ourselves properties with which to critique prior work, so let's use 'em!  We critique user-space frameworks for not having some visibility, but we didn't state visibility as a key property of GPU resource management.  Also, we didn't say that the interface needs to be vendor-agnostic or "in-fragile" (whatever that even means: the word fragile is not defined in this context).  When you pick some properties with which to critique a system, *stick with those properties*!}

% \arq{This is a mostly good paragraph. But, it should reference all of the properties that we've chosen as necessary.  We didn't say visibility/precise control above (nor do I know what precise control means).  }

% We propose resolving this tradeoff by creating a programmable OS subsystem interface for GPU resource management that is full-stack, safe and dynamic. 

In this paper, we propose \sys, a new system that provides an OS interface for GPU resource policy management that is safe, dynamic, and full-stack.  \sys{}'s key design feature is that it supports eBPF-based policies: users write callback functions that manage system resources and are called on user-defined triggers (e.g., when the system reaches a specific code locations, every second, etc.).  \sys{}'s use of eBPF ensures safety, since the system uses an eBPF-style verifier before running new policies, and dynamism, since it can replace policies without having to restart service interruptions.  To provide a full-stack interface, \sys{} supports eBPF callbacks that execute with in the OS kernel driver and GPU device.
%To provide a full-stack interface, \sys{} supports eBPF policies that use triggers from applications, the OS GPU kernel driver, and the GPU device and supports callbacks that execute on both the host and device.

% To ensure safety and enable dynamic policy updates, we leverage eBPF, an OS kernel extensibility mechanism previously used in CPU scheduling~\cite{schedext-docs}, page caching~\cite{zussman2025cache_ext}, network management~\cite{bachl2021flow,Zhong22}, and storage systems~\cite{jia2023programmable}. eBPF allows verified user-defined callbacks to safely and dynamically replace hardcoded OS policies without service interruptions.\sys{} integrates directly within the GPU's OS kernel driver, which enables a full-stack interface since it can control cross-tenant hardware resources and coordinate across applications. 

% \arq{The citations in this last sentence are a bit awkward. I understand why you want to cite that others have used eBPF, but we just need to rewrite the sentence.}

% \arq{The topic sentence is challenging to parse here. I would simplify the paragraph's start to be "Alas, applying eBPF to GPU resource management is not straightforward because the GPU execution model and resources are fundamentally different from those from CPUs.  In particular, we identify three challenges: " }
% \yusheng{I think what we may want is to say whay instead of just say different?}

Alas, applying eBPF to GPU resource management is not straightforward. The GPU is a discrete processor with its own execution model and resources, and policy decisions made in the host take effect on a separate device. \arq{Why wasn't that previous sentence one of the challenges?}  In particular, we identify three challenges: first, cross-device operations like migrating data or dispatching commands take milliseconds, too slow for synchronous callbacks and too costly for purely reactive decisions. Second, unlike independent subsystems on the CPU, limited device memory and bandwidth further couple memory and scheduling, as decisions about data placement directly affect which workloads can execute.   Third, GPU applications employ kernel bypass, which requires \sys{} to support eBPF callbacks from triggers in userspace applications.  But, traditional eBPF design does not allow such callbacks to modify the OS state required to implement resource management policies.


% \arq{eBPF does not support user-defined triggers?}
%Third, bypass the OS kernel:  and application-level intent is hidden from the driver since user-space runtimes bypass the OS kernel.

% \arq{This first sentence has no meaning to me.  What does it mean to be an effect on resource lifecycles? What does it mean to direct transitions? How are the effects of resource lifecycles related to the idea of directing transitions?}
% \yusheng{Resolved: rewrote to "decouple policy triggers from their effects" with concrete state machine example.}


% To address these challenges, the key insight of \sys{} is to decouple policy triggers from their effects (\S\ref{sec:design-interfaces}): \sys{} models GPU resource management as programmable, asynchronous transitions on resource state machines.       State machines define valid states and transitions: GPU memory chunks transition between host memory and device memory through eviction and prefetch; compute contexts transition through states such as running, ready via scheduling and preemption operations.
% Unlike sched\_ext\cite{schedext-docs}, cache\_ext\cite{zussman2025cache_ext}, and XDP~\cite{bachl2021flow}, where each callback makes one synchronous decision at one fixed transition point, \sys{} policies attach to user-defined points across the stack, and each trigger can initiate multiple state transitions asynchronously. To ensure safety, policies only influence which transitions occur and when, but cannot create invalid transitions or violate resource invariants. \sys{} extends this model to GPU-internal execution, where a SIMT-aware verifier enables verified eBPF to run on GPU hardware for observation and policy execution (\S\ref{sec:verification}). 

\sys{} addresses these challenges through a novel execution model for eBPF-based resource management that separates the effect of a callback from the trigger of the callback.  Unlike prior eBPF-based resource management (sched\_ext\cite{schedext-docs}, cache\_ext\cite{zussman2025cache_ext}, and XDP~\cite{bachl2021flow}), where each callback makes a single synchronous decision at fixed triggers, \sys{} allows callbacks that asynchronously transition resources at user-defined triggers. 
% To address these challenges, \sys's execution model differs from prior eBPF-based resource management~\cite{schedext-docs, zussman2025cache_ext, bachl2021flow} in that it separates the effects of a policy callback from the trigger of the policy callback.  
Namely, the system models GPU resource management policies as programmable, asynchronous transitions on resource state machines (\S\ref{sec:design-interfaces}).  State machines define valid states and transitions: GPU memory chunks transition between host memory and device memory through eviction and prefetching; compute contexts transition through states such as running, ready, and finished via scheduling and preemption operations.  
% Unlike sched\_ext\cite{schedext-docs}, cache\_ext\cite{zussman2025cache_ext}, and XDP~\cite{bachl2021flow}, where each callback makes one synchronous decision at one fixed transition point,
\sys callbacks invoke transitions when a given trigger is reached and the system asynchronously transitions the resource between states.  \sys{} callbacks can initiate multiple state transitions within a single trigger.  Finally, \sys{} supports user-defined triggers, which enables callbacks to be triggered from the application, OS GPU driver, or the GPU device.


% To ensure safety, policies only chose which transitions occur and when they occur, but cannot initiate invalid transitions that would violate the resource's state machine.  \sys{} enables callbacks.  \sys{} provides three features that diverge from prior work on eBPF-based resource management~\cite{}: First, \sys{} callbacks can initiate multiple state transitions at once.  Second, \sys{} allows. Unlike sched\_ext\cite{schedext-docs}, cache\_ext\cite{zussman2025cache_ext}, and XDP~\cite{bachl2021flow}, where each eBPF callback can only make a single decision (i.e., cause one transition), \sys{} callbacks can initiate multiple state transitions at once.   \sys{} extends this model to GPU-internal execution, where a SIMT-aware verifier enables verified eBPF to run on GPU hardware for observation and policy execution (\S\ref{sec:verification}). 


%decouple policy triggers from their effects (\S\ref{sec:design-interfaces}): \sys{} models GPU resource management as programmable, asynchronous transitions on resource state machines. 



% \arq{The way that this is written is confusing.  I think the key idea is to think of each GPU resource as having an associated asynchronous state machine.  For example, GPU memory has two possible states (residing in GPU memory or CPU memory) and has two transitions (prefetch, which moves from CPU memory to GPU, and eviction, which moves from GPU memory to CPU).  Is that what this paragraph is trying to say?}
% \yusheng{Resolved: now explicitly uses state machine framing with host memory/device memory states and eviction/prefetch transitions.}

We implement \sys{} on Linux by extending the NVIDIA open GPU OS kernel modules~\cite{nvidia-open-gpu} and providing an eBPF runtime for both host driver and GPU device. We evaluate \sys{} across LLM inference, GNN training, vector search, and multi-tenant scenarios. \sys-based policies improve throughput by up to $4.8\times$ over framework offloading and $1.76\times$ over application-tuned baselines, and reduce tail latency by up to $2\times$, with low overhead and no application modifications or restarts.
In our experiments, AI agents generated all 59 evaluated BPF policies, including eviction ordering, prefetch strategies, scheduling logic, and device-side work-stealing, with zero OS kernel panics, validating that eBPF's safety guarantees extend to the GPU driver and device. In summary, our contributions are:

\begin{itemize}[leftmargin=*,itemsep=0pt]
  \item We design a GPU-specific OS policy interface that models GPU resources as state machines whose asynchronous state transitions are directed by verified BPF programs across the host-device boundary.
  \item We implement \sys, an eBPF runtime spanning host driver and GPU device, with a SIMT-aware verifier for device-side BPF execution.
  \item We evaluate across four GPU workloads, achieving up to $4.8\times$ throughput improvement and $2\times$ tail latency reduction, with all 59 policies generated by AI agents and no OS kernel panics.
\end{itemize}
