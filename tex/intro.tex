\begin{abstract}
GPU resource management policies critically influence application performance, yet optimal policies vary widely with workloads and deployment contexts.
Current user-space solutions offer programmability and flexibility but lack cross-tenant visibility and hardware control, whereas kernel-driver modifications introduce complexity and safety risks, limiting their suitability for AI agent policy exploration.
We introduce \sys, an OS interface for GPU resource management that resolves this tradeoff through safe, programmable eBPF extensions across GPU kernel driver and device. Unlike existing CPU-focused policy interfaces which rely on synchronous callbacks and local resource control, \sys addresses GPU-specific challenges: high latency of cross-device coordination, tight coupling of memory management and scheduling decisions, and limited device observability. \sys models GPU resource management as asynchronous, programmable lifecycles directed by verified eBPF programs, decoupling policy triggers from lifecycle effects across the device boundary. To further bridge the visibility gap, \sys enables safe eBPF execution directly on GPU hardware with a SIMT-aware verifier. We prototype \sys within the Linux NVIDIA GPU driver, evaluating it across diverse workloads including LLM inference, GNN training, vector search, and multi-tenant scenarios. AI-generated \sys policies consistently outperform default strategies, improving throughput by up to 4.8× and reducing tail latency by up to 2×, all without application modifications or stability issues.

% To address this, we argue that the GPU driver and device layer should provide an extensible OS interface for policy enforcement. While the emerging eBPF technology shows potential, directly applying existing host-side eBPF is insufficient because they lack visibility and control into critical device-side events, and directly embedding policy code into GPU kernels could compromise safety and efficiency. 
% \arq{We said that we want an extensible OS interface, what's the connection to this second sentence?  Why are we bringing up eBPF? Why does eBPF have these issues?  There's a big gap between the previous sentence and this one}
% We propose \sys, an eBPF-based runtime that treats the GPU driver and device as a programmable OS subsystem. \sys extends GPU drivers by exposing safe programmable hooks and introduces a device-side eBPF runtime capable of executing verified policy logic within GPU kernels, enabling coherent and transparent policies. Evaluation across realistic workloads including inference, training, and vector search demonstrates that \sys improves throughput by up to $4.8\times$ and reduces tail latency by up to $2\times$, incurring minimal overhead, without modifying or restarting applications.
\end{abstract}

\section{Introduction}

% P1: GPU policy impact + diversity
% \arq{Save the agent talk for a future paragraph since its a separate idea}
% \arq{You have to define UVM}
% \arq{This sentence is trying to do too much. It says that default policies perform worse for a number of use-cases, argues that prior work shows for scheduling and memory management, and claims that the optimzl choice varies by deployment.  This should be at least three sentences.}
% \arq{This is a good citation/argument}
GPU resource management policies have large, workload-dependent performance impact. Default memory and scheduling policies perform poorly across diverse deployments spanning inference~\cite{kwon2023efficient,agrawal2024taming}, training~\cite{shoeybi2019megatron,rasley2020deepspeed}, graph analytics~\cite{lin2024towards,wang2016gunrock}, and vector search~\cite{pan2024survey,cao2023gpu}. Recent work confirms this for Unified Virtual Memory (UVM) placement~\cite{wang2024suv,lin2025forest,kim2025dream,park2025helm} and GPU scheduling~\cite{wang2024gcaps,fan2025gpreempt,shen2025xsched}, and the optimal choice varies with workload, hardware, and deployment context. Under memory oversubscription, for example, the choice of eviction and prefetch policy alone controls up to 73\% of execution time~\cite{park2025helm,kehne2019etc,ganguly2019interplay}.

% P1.5: Why agent-based optimization fits this domain
% \arq{The "agent stuff" feels very bolted on at this point in the paper.  It reads as though we wanted to make the paper "hot" so we chucked on some agent stuff so that we could say we have an AI agent paper. We need to change the approach if we want to make it a core part of the story.}
% \arq{The way I would change it is to have a paragraph about why Agent-based optimization is a good idea for this domain *before* talking about the issue with current mechanisms for resource management programmability.}
At the same time, AI agents are increasingly capable of writing system policy code, not merely tuning configurations~\cite{cheng2025barbarians,hamadanian2025glia,liu2025systemint}. Recent work demonstrates agents generating compiler heuristics~\cite{alphaevolve2025}, datacenter scheduling and LLM serving code~\cite{hamadanian2025glia,cheng2025barbarians}, eBPF policies~\cite{dwivedula2025policysmith,zheng2025schedcp}, and training configurations~\cite{ding2025asap}. To be effective, these agents require a programmable and safe interface for policy execution.
%  GPU resource management, with its large policy space and workload-dependent optima, is a natural target for agent-driven policy exploration.

% P2: Tradeoff between existing approaches
% \arq{I don't think this is true.  In the abstract (and below) we claim that the current approach to writing policies has a tradeoff where user-space runtimes provide safety but limit visibility while OS kernel runtimes provide visibility but limit safety.  This paragraph should stick with articulating that tradeoff, rather than trying to point to what an alternative might look like (i.e., a programmable OS extensibility interface).}
% \arq{The driver discussion is a different idea than where this paragraph starts.  We aren't ready to get into the discussion of the driver at this point in the paper.}
% \arq{I like the idea of a tradeoff between current user-space approaches and current "modify the kernel" approaches. Frame this paper that way.}
% \arq{I don't think we should bring up GPU profiling frameworks, they feel orthogonal to the point of the paragraph}
For GPU resource management, however, existing approaches present a tradeoff. User-space frameworks~\cite{ng2023paella,shen2025xsched,gim2025pie,chen2025ktransformers} offer programmability but lack cross-application visibility and low-level GPU control such as page fault handling and command scheduling~\cite{fan2025gpreempt,nvidia-open-gpu}. Kernel driver modifications~\cite{kato2011timegraph,kato2012gdev,kernelintercept,fan2025gpreempt,wang2024gcaps} provide visibility and control but are fragile, vendor-specific, and unsafe for iterative or agent-generated code. Fixed configuration knobs and advisory hints exposed by existing APIs are safe but limit what agents can express.
% GPU profiling frameworks~\cite{cupti,villa2019nvbit,neutrino} enable observation but lack runtime programmability.
% Original: GPU resource management policy is scattered across user-space frameworks, the CUDA runtime, and the kernel driver [moved to background]
% \arq{I don't think we should bring up GPU profiling frameworks, they feel orthogonal to the point of the paragraph}

% P3: eBPF breaks the tradeoff
% \arq{I dont' think we're proposing "treating" GPU resource management as a programmable OS subsystem, we're proposing *creating* a programmable GPU resource management system.  I think we need to say something deeper and more precise about our goals: that we want to create a programmable subsystem for GPU resource management that breaks the tradeoff (ideally, we introduce this tradeoff above) between visibility and safety.  To ensure visibility, our resource management subsystem will support programmability from within the operating system.  To ensure safety, our resource management subsystem will turn to eBPF.}
% \arq{Are these key properties that we need for gpu resource management?}
% \arq{This is an awkward inclusion at the end of the paragraph. It either should be stated when we introduce eBPF "Like prior work on scheduling and file systems, we argue for eBPF as a feasible approach to ensure an safe OS-level programmable interface}
We create a programmable OS interface for GPU resource management that breaks this tradeoff. For visibility and control, our subsystem operates within the GPU kernel driver, the natural coordination point providing cross-tenant control and hardware privilege over page tables and scheduling. For safety, we turn to eBPF: like sched\_ext for CPU scheduling~\cite{schedext-docs} and cache\_ext for page caching~\cite{zussman2025cache_ext}, we use verified BPF programs to replace hardcoded driver policies at runtime, with guaranteed safety and automatic fallback to default behavior~\cite{bachl2021flow,Zhong22,jia2023programmable}.

% P4a: Co-location insight + two mismatches
However, the GPU is a discrete processor with its own execution model and resources, and policy decisions in the host driver must take effect on a separate device, making direct application of the CPU model insufficient: 
cross-device operations like migrating data or dispatching commands take milliseconds, too slow for synchronous callbacks and too costly for purely reactive decisions. Limited device memory and bandwidth further couple memory and scheduling, as decisions about data  placement directly affect which workloads can execute. Finally, the GPU's distinct architecture also makes its internal execution state opaque to the host without device-side instrumentation. %, while existing frameworks~\cite{cupti,villa2019nvbit,neutrino}..
% \arq{There are some good ideas in this paragraph, although it is pretty complicated to disentangle. I think that we need to rework the paragraph keeping in mind that the audience does not know what eBPF is, what advisory hooks are, what proactive preemption is, etc.}

% P4b: Mechanism mapping
% \arq{The content is generally strong, but I think this needs to be "punchier". In part, this will happen when we work out the previous paragraph more because the two are in conversation with eacy other.}
To address these challenges, \sys{} models GPU resource management as programmable lifecycles, where verified BPF programs direct transitions of both memory and scheduling via effects (\S\ref{sec:design-interfaces}). Unlike current policy interfaces such as sched\_ext, cache\_ext and XDP, where each callback makes one synchronous decision at one transition point, \sys{} programs can direct multiple transitions asynchronously from any trigger in the management stack, decoupling triggers from their effects to bridge the cross-device timescale gap. To ensure safety, policies only influence which transitions occur and when, but cannot create invalid transitions or violate resource invariants. \sys{} extends this model to GPU-internal execution, where a SIMT-aware verifier enables verified BPF to run on GPU hardware for observation and policy execution (\S\ref{sec:verification}).

% P5: System summary (merged into P4b above)
We implement \sys{} on Linux by extending the NVIDIA open GPU kernel modules~\cite{nvidia-open-gpu} and providing an eBPF runtime for both host driver and GPU device. We evaluate \sys{} across LLM inference, GNN training, vector search, and multi-tenant scenarios. \sys-based policies improve throughput by up to $4.8\times$ and reduce tail latency by up to $2\times$ compared to original policies, with low overhead and no application modifications or restarts.
%~\arq{It is unconventional (and, somewhat unwarranted) to preview results from the ablation study in the introduction.}
In our experiments, AI agents generated all 59 evaluated BPF policies, including eviction ordering, prefetch strategies, scheduling logic, and device-side work-stealing, with zero kernel panics, validating that eBPF's safety guarantees extend to the GPU driver and device. In summary, our contributions are:

\begin{itemize}[leftmargin=*,itemsep=0pt]
  \item We design an extensible OS policy interface for GPU resource management.

  \item We implement \sys, an eBPF runtime spanning host driver and GPU device, with a SIMT-aware verifier for device-side BPF execution.

  \item We evaluate across four GPU workloads, achieving up to $4.8\times$ throughput improvement and $2\times$ tail latency reduction, with all 59 policies generated by AI agents and zero kernel panics.
\end{itemize}

% \arq{We should talk about whether we want to stick with these three things as our contribution.  My gut suggests that the first one is not that strong.  I am curious if we can make one of the ideas from the background/motivation into a fully fledged contribution here (?)}