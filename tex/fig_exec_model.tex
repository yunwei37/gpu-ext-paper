\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=0.35cm and 0.6cm,
  box/.style={draw, rounded corners, minimum height=0.6cm, minimum width=1.3cm, font=\small},
  trigger/.style={box, fill=blue!10},
  bpf/.style={box, fill=green!10},
  effect/.style={box, fill=orange!10},
  label/.style={font=\small\itshape, text=gray},
  arrow/.style={->, >=stealth, semithick},
  dasharrow/.style={->, >=stealth, semithick, dashed},
  every node/.style={font=\small},
]

% === sched_ext (top) ===
\node[font=\small\bfseries] (sched_title) at (0, 0) {sched\_ext / cache\_ext / XDP (1:1, sync, same processor):};

\node[trigger] (s_trans) at (-2.3, -0.7) {transition};
\node[bpf]     (s_bpf)   at (0, -0.7)    {BPF};
\node[effect]  (s_eff)   at (2.5, -0.7)   {immediate effect};

\draw[arrow] (s_trans) -- (s_bpf);
\draw[arrow] (s_bpf)   -- (s_eff);

% === gpu_ext (bottom) ===
\node[font=\small\bfseries] (gpu_title) at (0, -1.5) {\sys{} (many:many, async, cross-device):};

% Host triggers
\node[trigger] (t_a) at (-2.9, -2.3) {driver hook};
\node[trigger] (t_b) at (-2.9, -2.95) {uprobe};
\node[trigger] (t_c) at (-2.9, -3.6) {bpf\_wq};

% BPF + maps
\node[bpf, minimum height=1.4cm, minimum width=1.3cm, text width=1.1cm, align=center] (g_bpf) at (-0.3, -2.95) {BPF\\+ maps};

% Effects (state transitions)
\node[effect] (e_x) at (2.5, -2.3) {prefetch};
\node[label, right=0.05cm of e_x] {\small async};
\node[effect] (e_y) at (2.5, -2.95) {evict};
\node[label, right=0.05cm of e_y] {\small reorder};
\node[effect] (e_z) at (2.5, -3.6) {preempt};
\node[label, right=0.05cm of e_z] {\small select};

% Device boundary (polyline: L-shape under triggers, up between BPF and effects)
\draw[thick, dashed, gray]
  (-3.8, -4.0) -- (0.9, -4.0) -- (0.9, -1.9);
\node[font=\small\itshape, text=gray, align=center] at (0.9, -4.3) {device boundary};

% Device trigger (below boundary, left)
\node[trigger, fill=purple!10] (t_d) at (-2.9, -4.5) {device event};

% Arrows from triggers to BPF
\draw[arrow] (t_a) -- (g_bpf);
\draw[arrow] (t_b) -- (g_bpf);
\draw[arrow] (t_c) -- (g_bpf);
\draw[dasharrow] (t_d) -- (g_bpf);

% Arrows from BPF to effects (cross boundary)
\draw[dasharrow] (g_bpf) -- (e_x);
\draw[dasharrow] (g_bpf) -- (e_y);
\draw[dasharrow] (g_bpf) -- (e_z);

% Labels
\node[label] at (-2.9, -1.9) {triggers (open)};
\node[label] at (2.5, -1.9) {effects (bounded)};

% State-transition boundary box
\draw[thick, dotted, orange!60] (1.4, -2.0) rectangle (4.3, -3.95);
\node[font=\small, orange!80!black, rotate=90] at (4.5, -2.95) {state transitions};

\end{tikzpicture}
\caption{Policy execution model: sched\_ext / cache\_ext / XDP vs \sys{}.\arq{This figure does not make a lot of sense to me, and I don't think that it is adequently explained.  It also uses different words than the rest of the paper, which makes it very confusing.}}
\label{fig:exec-model}
\end{figure}
